<!-- <h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th>nome completo</th>
      <th>e-mail</th>
      <th>Location</th>
      <th>bio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.full_name %></td>
        <td><%= user.email %></td>
        <td><%= user.location %></td>
        <td><%= user.bio %></td>
           <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Editar', edit_user_path(user) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo cadastro', new_user_path %>
<%= link_to "Voltar", root_path %> -->

 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Colcho.net</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="">
</head>
<body>
<h2></h2>
<button type="button" class="btn btn-primary btn-lg btn-block"><h2>Listar usuarios</h2></button>
  <div class="row clearfix">
    <div class="col-md-12 column">
      <table class="table">
        <thead>
          <tr>
            <th>Nome completo</th>
            <th>E-mail</th>
            <th>local</th>
            <th>biografia</th>
            <th colspan="3"></th>
          </tr>
        </thead>

     <% @users.each do |user| %>
        
        <tbody>
          <tr class="active">
            <td>
              <li><%= link_to "#{user.full_name}",user %> </li>
            </td>
            <td>
              <li><%= "#{user.email}" %> </li>
            </td>
            <td>
              <li><%= "#{user.location}" %> </li>
            </td>
            <td>
              <li><%= "#{user.bio}" %> </li>
            </td>
            <td><%= link_to 'Visualizar', user %></td>
            <td><%= link_to 'Editar', edit_user_path(user) %></td>
            <td><%= link_to 'Excluir',user, method: :delete, data: { confirm: 'tem certeza?' } %></td>
          </tr>
        </tbody>
    <% end %> 
      </table>
    </div>
  </div>
    <a class="btn btn-default" <%= link_to 'cadastrar usuario', new_user_path %></a>
    <a class="btn btn-default" <%= link_to "Voltar", root_path %> </a>
</body>
</html>